# BP-Chat Production Environment Configuration
# Copy this to .env for production deployment

# Environment
ENVIRONMENT=production

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Database Configuration
# Option 1: Use existing Agion PostgreSQL (RECOMMENDED)
# Replace with your actual Agion PostgreSQL connection string
DATABASE_URL=postgresql+asyncpg://bpchat_user:password@your-agion-postgres.postgres.database.azure.com:5432/bpchat_db?sslmode=require

# Option 2: Use SQLite for simple deployment (not recommended for production)
# DATABASE_URL=sqlite+aiosqlite:///./bpchat.db

# CORS (Update with your frontend domains)
CORS_ORIGINS=["https://your-frontend-domain.com", "https://www.your-frontend-domain.com"]

# API Keys (REQUIRED - Use Azure Key Vault or secure environment variables)
REQUESTY_AI_API_KEY=${REQUESTY_AI_API_KEY}
REQUESTY_AI_API_BASE=https://router.requesty.ai/v1

# Security (REQUIRED - Generate a strong secret key)
JWT_SECRET_KEY=${JWT_SECRET_KEY}

# File Storage - Azure Blob Storage (Recommended for production)
STORAGE_BACKEND=azure

# Azure Storage Configuration (REQUIRED for Azure storage)
AZURE_STORAGE_CONNECTION_STRING=${AZURE_STORAGE_CONNECTION_STRING}
AZURE_STORAGE_CONTAINER_NAME=bpchat-files
AZURE_STORAGE_SAS_EXPIRY_DAYS=7

# Performance Settings
PYDANTIC_DISABLE_PLUGINS=1  # Prevents timeout issues
PYTHONUNBUFFERED=1  # Ensures logs are displayed immediately

# Monitoring (Optional)
ENABLE_METRICS=true
METRICS_PORT=9090

# Rate Limiting
RATE_LIMIT_REQUESTS=1000
RATE_LIMIT_WINDOW=60

# Cache Configuration (Optional Redis)
# REDIS_URL=redis://default:password@redis-server:6379/0
# ENABLE_CACHING=true
# CACHE_TTL_SECONDS=3600
